# Unity Example

This example is optional. It is not used by the default starter config.

Use this config when you want a Unity-oriented example catalog:

```bash
command-atlas-mcp --config ./examples/unity/command-atlas.config.json
```

The commands live under `./examples/unity/commands/scene`.