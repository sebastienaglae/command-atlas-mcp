# Blender Example

This example is optional. It is not used by the default starter config.

Use this config when you want a Blender-oriented example catalog:

```bash
command-atlas-mcp --config ./examples/blender/command-atlas.config.json
```

The commands live under `./examples/blender/commands/assets`.